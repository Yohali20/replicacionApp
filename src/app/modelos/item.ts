export class Item{
    id: number=0; //id
    title: string=''; //titulo
    price: number=0; //precio
    quantity: number=0; //cantidad
    completed: boolean=false; //si ya se pago por ese producto 
}